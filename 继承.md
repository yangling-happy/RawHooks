# 继承

## 一、继承是什么？为什么需要继承？

**先理解两个生活场景：**

- **继承**：就像孩子继承父母的基因。孩子天然拥有父母的特征（身高、肤色），同时还可以有自己的独特特征（才艺、性格）。在编程中，子类继承父类的属性和方法，避免重复代码。

- **原型链**：就像家族族谱。当你问一个人会不会某项技能时，他会先看自己会不会，如果不会就翻族谱往上找祖先有没有，直到找到或查无此人。

## 二、五种继承方式代码逐行解析

```javascript
/**
 * 题目：实现 JavaScript 继承
 * 要求：分别用以下方式实现继承，并说明各自的优缺点
 * 1. 原型链继承
 * 2. 借用构造函数继承
 * 3. 组合继承
 * 4. 寄生组合继承（最优方案）
 * 5. ES6 class 继承
 */

// 定义一个父类（构造函数）
function Animal(name) {
    this.name = name;
    this.colors = ['black', 'white']; // 引用类型属性，用于测试问题
}

// 在父类的原型上添加一个方法
Animal.prototype.sayName = function() {
    console.log(`My name is ${this.name}`);
};

// ==================== 1. 原型链继承 ====================
console.log('========== 1. 原型链继承 ==========');

function Dog() {}
// 核心：子类的原型指向父类的实例
Dog.prototype = new Animal();

// 修复构造函数指向（否则 Dog.prototype.constructor 会指向 Animal）
Dog.prototype.constructor = Dog;

// 添加子类自己的方法
Dog.prototype.bark = function() {
    console.log('Woof!');
};

const dog1 = new Dog();
dog1.name = 'Buddy';
dog1.colors.push('brown');
console.log(dog1.colors); // ['black', 'white', 'brown']

const dog2 = new Dog();
dog2.name = 'Max';
console.log(dog2.colors); // ['black', 'white', 'brown']  ⚠️ 所有实例共享 colors！

dog1.sayName(); // My name is Buddy
dog1.bark();    // Woof!

/*
 * 优点：
 *   - 实现简单，易于理解
 *   - 子类可以访问父类原型上的方法和属性
 *   - 可以复用父类构造函数中的方法
 * 
 * 缺点：
 *   - 引用类型属性（如 colors）会被所有子类实例共享，一个实例修改会影响其他实例
 *   - 无法向父类构造函数传递参数（所有实例的父类属性都是一样的）
 *   - 创建子类实例时无法自定义父类属性的初始值
 */

// ==================== 2. 借用构造函数继承 ====================
console.log('\n========== 2. 借用构造函数继承 ==========');

function Cat(name) {
    // 核心：在子类构造函数中调用父类构造函数，并绑定 this
    Animal.call(this, name);
}

const cat1 = new Cat('Tom');
cat1.colors.push('gray');
console.log(cat1.colors); // ['black', 'white', 'gray']
console.log(cat1.name);   // Tom

const cat2 = new Cat('Jerry');
console.log(cat2.colors); // ['black', 'white']  ✅ 不受 cat1 影响
console.log(cat2.name);   // Jerry

// cat1.sayName();  // ❌ TypeError: cat1.sayName is not a function（无法访问父类原型方法）

/*
 * 优点：
 *   - 解决了原型链继承中引用类型共享的问题
 *   - 可以向父类构造函数传递参数
 *   - 每个子类实例都有独立的父类属性副本
 * 
 * 缺点：
 *   - 无法继承父类原型上的方法（如 sayName），只能继承构造函数中定义的属性
 *   - 方法都在构造函数中定义，无法复用，造成内存浪费
 *   - 子类实例无法访问父类原型链上的方法
 */

// ==================== 3. 组合继承 ====================
console.log('\n========== 3. 组合继承 ==========');

function Bird(name) {
    // 第二次调用 Animal：创建实例属性（name, colors）
    Animal.call(this, name);
}

// 第一次调用 Animal：设置原型链
Bird.prototype = new Animal();
Bird.prototype.constructor = Bird;

// 添加子类自己的方法
Bird.prototype.fly = function() {
    console.log(`${this.name} is flying`);
};

const bird1 = new Bird('Sparrow');
bird1.colors.push('yellow');
console.log(bird1.colors); // ['black', 'white', 'yellow']
console.log(bird1.name);   // Sparrow
bird1.sayName();           // My name is Sparrow
bird1.fly();               // Sparrow is flying

const bird2 = new Bird('Eagle');
console.log(bird2.colors); // ['black', 'white']  ✅ 不受影响
bird2.sayName();           // My name is Eagle

/*
 * 优点：
 *   - 结合了原型链继承和借用构造函数的优点
 *   - 可以继承父类原型上的方法，也拥有独立的实例属性
 *   - 可以向父类传递参数
 *   - 最常用的继承方式之一
 * 
 * 缺点：
 *   - 会调用两次父类构造函数（一次在设置原型时，一次在实例化时）
 *   - 子类原型上会多出一份不必要的父类实例属性（造成内存浪费）
 *   - 虽然实例属性会覆盖原型上的同名属性，但原型上仍然存在这些属性
 */

// ==================== 4. 寄生组合继承（最优方案） ====================
console.log('\n========== 4. 寄生组合继承 ==========');

function Fish(name) {
    // 只调用一次父类构造函数
    Animal.call(this, name);
}

// 核心：使用 Object.create() 创建父类原型的副本，避免调用父类构造函数
function inheritPrototype(subType, superType) {
    // 创建一个空对象，其原型指向 superType.prototype
    const prototype = Object.create(superType.prototype);
    // 修复构造函数指向
    prototype.constructor = subType;
    // 设置子类的原型
    subType.prototype = prototype;
}

inheritPrototype(Fish, Animal);

// 添加子类自己的方法
Fish.prototype.swim = function() {
    console.log(`${this.name} is swimming`);
};

const fish1 = new Fish('Nemo');
fish1.colors.push('orange');
console.log(fish1.colors); // ['black', 'white', 'orange']
console.log(fish1.name);   // Nemo
fish1.sayName();           // My name is Nemo
fish1.swim();              // Nemo is swimming

const fish2 = new Fish('Dory');
console.log(fish2.colors); // ['black', 'white']  ✅ 独立
console.log(fish2.name);   // Dory

console.log(Fish.prototype.constructor === Fish); // true
console.log(fish1 instanceof Animal); // true
console.log(fish1 instanceof Fish);   // true

/*
 * 优点：
 *   - 只调用了一次父类构造函数（解决了组合继承的缺点）
 *   - 原型链上不会有多余的父类实例属性
 *   - 保持了原型链的完整性
 *   - 是 ES5 中最理想的继承方案，也是很多框架（如 YUI）使用的方案
 * 
 * 缺点：
 *   - 实现相对复杂，需要额外的工具函数
 *   - 需要理解 Object.create() 和原型链的细节
 */

// ==================== 5. ES6 class 继承 ====================
console.log('\n========== 5. ES6 class 继承 ==========');

class Mammal {
    constructor(name) {
        this.name = name;
        this.colors = ['black', 'white'];
    }
    
    sayName() {
        console.log(`My name is ${this.name}`);
    }
    
    // 静态方法
    static getClassName() {
        return 'Mammal';
    }
}

class Whale extends Mammal {
    constructor(name, size) {
        // 必须调用 super() 才能使用 this
        super(name);  // 相当于调用父类的构造函数
        this.size = size;
    }
    
    // 重写父类方法
    sayName() {
        super.sayName(); // 调用父类方法
        console.log(`And I'm a whale`);
    }
    
    swim() {
        console.log(`${this.name} is swimming`);
    }
    
    // 静态方法也会被继承
    static getClassName() {
        return super.getClassName();
    }
}

const whale1 = new Whale('Blue', 'huge');
whale1.colors.push('blue');
console.log(whale1.colors);  // ['black', 'white', 'blue']
whale1.sayName();  // My name is Blue \n And I'm a whale
whale1.swim();     // Blue is swimming

console.log(whale1 instanceof Mammal);  // true
console.log(whale1 instanceof Whale);   // true
console.log(Whale.getClassName());      // Mammal

/*
 * 优点：
 *   - 语法简洁清晰，接近传统面向对象语言
 *   - 内部实现就是寄生组合继承，是最优方案的语法糖
 *   - 支持 super 关键字方便调用父类方法
 *   - 支持静态方法继承
 * 
 * 缺点：
 *   - 需要转译（Babel）才能在旧环境运行
 *   - 本质上还是原型链，不是真正的类
 *   - 必须使用 new 调用，不能作为普通函数
 */
```

## 三、五种继承方式对比流程图解

```
┌─────────────────────────────────────────────────────────────────┐
│                       1. 原型链继承                              │
├─────────────────────────────────────────────────────────────────┤
│  Dog.prototype = new Animal()                                    │
│                                                                  │
│  dog1 ──__proto__──→ Dog.prototype ──__proto__──→ Animal.prototype │
│         (实例)              (Animal实例)            (父类原型)     │
│                                                                  │
│  ⚠️ 问题：所有 dog 实例共享 Animal 实例中的 colors 数组           │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                    2. 借用构造函数继承                           │
├─────────────────────────────────────────────────────────────────┤
│  function Cat(name) { Animal.call(this, name); }                │
│                                                                  │
│  cat1 ──__proto__──→ Cat.prototype ──__proto__──→ Object.prototype │
│  (有独立属性副本)           (空)                    (无sayName)   │
│                                                                  │
│  ⚠️ 问题：无法访问 Animal.prototype 上的 sayName 方法            │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                       3. 组合继承                                │
├─────────────────────────────────────────────────────────────────┤
│  Bird.prototype = new Animal()  // 第一次调用 Animal            │
│  function Bird() { Animal.call(this) }  // 第二次调用           │
│                                                                  │
│  bird ──__proto__──→ Bird.prototype ──__proto__──→ Animal.prototype │
│                     (包含无用的Animal属性)          (sayName)     │
│                                                                  │
│  ⚠️ 问题：原型上多了一份无用属性，浪费内存                        │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                    4. 寄生组合继承（最优）                        │
├─────────────────────────────────────────────────────────────────┤
│  prototype = Object.create(Animal.prototype)  // 不调用Animal   │
│  Fish.prototype = prototype                                      │
│  function Fish() { Animal.call(this) }  // 只调用一次            │
│                                                                  │
│  fish ──__proto__──→ Fish.prototype ──__proto__──→ Animal.prototype │
│                     (纯净原型，无多余属性)            (sayName)     │
│                                                                  │
│  ✅ 完美：只调用一次父类，原型链干净整洁                          │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                      5. ES6 class 继承                           │
├─────────────────────────────────────────────────────────────────┤
│  class Whale extends Mammal {                                    │
│      constructor() { super(); }                                  │
│  }                                                               │
│                                                                  │
│  内部实现 = 寄生组合继承的语法糖                                  │
│  ✅ 语法最简洁，开发体验最好                                      │
└─────────────────────────────────────────────────────────────────┘
```

## 四、核心 API 详解

| API | 作用 | 在继承中的用途 |
|-----|------|----------------|
| `new Animal()` | 调用构造函数创建实例 | 原型链继承中作为子类原型 |
| `Parent.call(this)` | 在子类中执行父类构造函数 | 借用构造函数、组合、寄生组合继承 |
| `Object.create(proto)` | 创建以 proto 为原型的对象 | 寄生组合继承中复制父类原型 |
| `Child.prototype = Object.create(Parent.prototype)` | 建立原型链 | 寄生组合继承的核心 |
| `constructor` 修复 | 让原型上的 constructor 指向正确的构造函数 | 所有涉及原型替换的场景 |
| `super()` | 调用父类构造函数 | ES6 class 继承中 |

## 五、Object.create 在继承中的核心作用

### 5.1 为什么组合继承会调用两次父类？

```javascript
// 组合继承的问题演示
function Animal(name) {
    console.log('Animal 被调用');
    this.name = name;
}

function Dog(name) {
    Animal.call(this, name);  // 第二次调用：创建实例属性
}

Dog.prototype = new Animal();  // 第一次调用：设置原型（这里 name 为 undefined）

const dog = new Dog('旺财');
// 输出：
// Animal 被调用  (第一次，设置原型时)
// Animal 被调用  (第二次，new Dog 时)

// 问题：Dog.prototype 上有一份无用的 Animal 属性 { name: undefined, ... }
console.log(Dog.prototype.name);  // undefined（浪费内存）
```

### 5.2 寄生组合继承如何解决？

```javascript
// 寄生组合继承：用 Object.create 避免调用父类
function Animal(name) {
    console.log('Animal 被调用');
    this.name = name;
}

function Fish(name) {
    Animal.call(this, name);  // 只调用一次！
}

// 关键：不调用 Animal，只复制它的原型
Fish.prototype = Object.create(Animal.prototype);
Fish.prototype.constructor = Fish;

const fish = new Fish('Nemo');
// 输出：
// Animal 被调用  (只一次！)

// ✅ Fish.prototype 上没有任何多余的 Animal 实例属性
console.log(Fish.prototype.name);  // undefined（没有这个属性）
```

### 5.3 Object.create 手动实现（理解原理）

```javascript
// 手写 Object.create 帮助理解寄生组合继承
function myObjectCreate(proto) {
    function F() {}       // 空构造函数
    F.prototype = proto;  // 原型指向传入的对象
    return new F();       // 返回空实例，原型链已连接
}

// 所以 inheritPrototype 也可以这样写：
function inheritPrototype(subType, superType) {
    subType.prototype = myObjectCreate(superType.prototype);
    subType.prototype.constructor = subType;
}
```

## 六、五种继承方式详细对比表

| 特性 | 原型链 | 借用构造 | 组合继承 | 寄生组合 | ES6 class |
|------|--------|----------|----------|----------|-----------|
| **引用类型独立** | ❌ 共享 | ✅ 独立 | ✅ 独立 | ✅ 独立 | ✅ 独立 |
| **传递参数** | ❌ 不能 | ✅ 能 | ✅ 能 | ✅ 能 | ✅ 能 |
| **继承原型方法** | ✅ 能 | ❌ 不能 | ✅ 能 | ✅ 能 | ✅ 能 |
| **父类调用次数** | 1次 | 1次 | 2次 | 1次 | 1次 |
| **原型多余属性** | 无 | 无 | ⚠️ 有 | ✅ 无 | ✅ 无 |
| **代码复杂度** | 简单 | 简单 | 中等 | 复杂 | 极简 |
| **instanceof 正确** | ✅ 是 | ✅ 是 | ✅ 是 | ✅ 是 | ✅ 是 |
| **constructor 正确** | ⚠️ 需修复 | ✅ 自动 | ⚠️ 需修复 | ⚠️ 需修复 | ✅ 自动 |
| **推荐程度** | ❌ 不推荐 | ❌ 不推荐 | ⚠️ 可用 | ✅ 推荐 | ✅✅ 最推荐 |

## 七、内存与性能分析

### 7.1 组合继承的内存浪费

```javascript
// 组合继承：Bird.prototype 上有无用属性
function Animal(name) {
    this.name = name;
    this.colors = ['black', 'white'];
}

function Bird(name) {
    Animal.call(this, name);
}
Bird.prototype = new Animal();  // 这里创建了无用的 name 和 colors

const bird = new Bird('Sparrow');

// 内存中的情况：
// bird (实例) : { name: 'Sparrow', colors: [...] }
// Bird.prototype : { name: undefined, colors: [...], constructor: Bird }
//                     ↑ 这份 colors 数组永远用不到，浪费内存

console.log(bird.hasOwnProperty('name'));     // true（实例属性）
console.log(Bird.prototype.hasOwnProperty('name')); // true（浪费）
```

### 7.2 寄生组合继承的内存优化

```javascript
// 寄生组合继承：原型干净，无浪费
function Fish(name) {
    Animal.call(this, name);
}
Fish.prototype = Object.create(Animal.prototype);
Fish.prototype.constructor = Fish;

const fish = new Fish('Nemo');

// 内存中的情况：
// fish (实例) : { name: 'Nemo', colors: [...] }
// Fish.prototype : { constructor: Fish }  ← 只有 constructor，无多余属性
// Fish.prototype.__proto__ : { sayName: fn }  ← 父类原型

console.log(fish.hasOwnProperty('name'));        // true
console.log(Fish.prototype.hasOwnProperty('name')); // false（完美）
```

## 八、ES6 class 的 super 关键字详解

### 8.1 super 的两种用法

```javascript
class Parent {
    constructor(name) {
        this.name = name;
    }
    
    sayHello() {
        return `Hello from ${this.name}`;
    }
    
    static staticMethod() {
        return 'Parent static';
    }
}

class Child extends Parent {
    constructor(name, age) {
        super(name);  // 1️⃣ 作为函数调用：相当于 Parent.call(this, name)
        this.age = age;
    }
    
    sayHello() {
        // 2️⃣ 作为对象调用：指向父类原型
        const parentResult = super.sayHello();  // 调用父类原型方法
        return `${parentResult}, age ${this.age}`;
    }
    
    static staticMethod() {
        // 静态方法中 super 指向父类本身
        return super.staticMethod() + ' → Child';
    }
}

const child = new Child('Tom', 18);
console.log(child.sayHello());  // Hello from Tom, age 18
console.log(Child.staticMethod());  // Parent static → Child
```

### 8.2 super 的限制

```javascript
// ❌ 错误：不能在普通函数中使用 super
function notAllowed() {
    super();  // SyntaxError: 'super' keyword unexpected here
}

// ❌ 错误：必须在 constructor 中先调用 super 才能使用 this
class Wrong extends Parent {
    constructor(name) {
        this.name = name;  // ReferenceError: Must call super first
        super(name);
    }
}
```

## 九、边界情况与常见陷阱

### 9.1 忘记修复 constructor 指向

```javascript
function Animal() {}
function Dog() {}
Dog.prototype = new Animal();
// 忘记修复：Dog.prototype.constructor 现在是 Animal

const dog = new Dog();
console.log(dog.constructor === Dog);  // false！指向 Animal
console.log(dog.constructor === Animal);  // true

// 带来的问题：类型判断混乱
// 某些库依赖 constructor 来识别类型时会出错
```

### 9.2 子类方法覆盖父类方法

```javascript
function Animal() {}
Animal.prototype.say = function() { return 'Animal'; };

function Dog() {}
Dog.prototype = new Animal();
Dog.prototype.say = function() { 
    // 如果想调用父类方法，在 ES5 中需要这样做：
    const parentResult = Animal.prototype.say.call(this);
    return parentResult + ' → Dog';
};

const dog = new Dog();
console.log(dog.say());  // Animal → Dog

// ES6 中有 super，更优雅：
class DogClass extends Animal {
    say() {
        return super.say() + ' → Dog';
    }
}
```

### 9.3 不能继承普通对象

```javascript
const parent = { name: 'parent' };

// ❌ 以下写法都不对：
// function Child() {}
// Child.prototype = parent;  // 这是原型指向，不是真正的类继承

// ✅ 正确：使用 Object.create 实现对象继承
const child = Object.create(parent);
child.name = 'child';
console.log(child.name);  // 'child'（自己的属性）
console.log(child.__proto__.name);  // 'parent'

// 或者 ES6 的 Object.setPrototypeOf
const child2 = {};
Object.setPrototypeOf(child2, parent);
```

## 十、继承方案的选择建议

### 10.1 决策流程图

```
开始
  │
  ▼
是否支持 ES6？
  │
  ├─ 是 ──→ 使用 ES6 class 继承 ✅
  │          (最简洁，最优方案)
  │
  └─ 否 ──→ 是否必须兼容老旧浏览器？
              │
              ├─ 是 ──→ 使用寄生组合继承 ✅
              │          (性能最优，无冗余)
              │
              └─ 否/简单场景 ──→ 可以用组合继承 ⚠️
                                  (代码稍简单，但有微小性能损失)
```

### 10.2 实际项目代码模板

```javascript
// 推荐模板1：ES6（现代项目）
class Parent {
    constructor(name) {
        this.name = name;
    }
    
    method() {
        return this.name;
    }
}

class Child extends Parent {
    constructor(name, age) {
        super(name);
        this.age = age;
    }
    
    method() {
        return `${super.method()}, age ${this.age}`;
    }
}

// 推荐模板2：寄生组合继承（ES5 环境）
function Parent(name) {
    this.name = name;
}
Parent.prototype.method = function() {
    return this.name;
};

function Child(name, age) {
    Parent.call(this, name);
    this.age = age;
}

// 寄生组合继承工具函数
function inherit(Child, Parent) {
    Child.prototype = Object.create(Parent.prototype);
    Child.prototype.constructor = Child;
}

inherit(Child, Parent);

Child.prototype.method = function() {
    return Parent.prototype.method.call(this) + `, age ${this.age}`;
};
```

## 十一、常见错误与调试

### 11.1 错误1：原型链继承中修改引用类型的误解

```javascript
// ❌ 错误理解
function Animal() { this.colors = ['red']; }
function Dog() {}
Dog.prototype = new Animal();

const dog1 = new Dog();
const dog2 = new Dog();
dog1.colors.push('blue');
console.log(dog2.colors);  // ['red', 'blue'] ← 你以为没关系，实际有关

// ✅ 正确做法：使用寄生组合继承
function Fish() { Animal.call(this); }  // 每个实例独立
Fish.prototype = Object.create(Animal.prototype);
```

### 11.2 错误2：ES6 class 忘记写 super

```javascript
// ❌ 错误
class Child extends Parent {
    constructor() {
        // 忘记 super()
        this.name = 'child';  // ReferenceError
    }
}

// ✅ 正确
class Child extends Parent {
    constructor() {
        super();  // 即使不需要参数也要调用
        this.name = 'child';
    }
}
```

### 11.3 错误3：组合继承中方法覆盖时机错误

```javascript
// ❌ 错误顺序
function Bird(name) { Animal.call(this, name); }
Bird.prototype.fly = function() {};  // 先添加方法
Bird.prototype = new Animal();  // 后替换原型 → 方法丢失！

// ✅ 正确顺序
function Bird(name) { Animal.call(this, name); }
Bird.prototype = new Animal();  // 先替换原型
Bird.prototype.fly = function() {};  // 后添加方法
Bird.prototype.constructor = Bird;  // 修复 constructor
```

## 十二、与原生继承的对比例

| 特性 | 原生 class | 手写寄生组合 |
|------|------------|--------------|
| 语法简洁度 | ⭐⭐⭐⭐⭐ | ⭐⭐ |
| 性能 | 最优 | 接近最优 |
| 兼容性 | ES6+ | ES5+ |
| super 关键字 | ✅ 支持 | ❌ 需手动调用 |
| 静态方法继承 | ✅ 自动 | 需手动实现 |
| 调试体验 | 好 | 原型链复杂，调试困难 |
| 学习成本 | 低 | 高 |

## 十三、总结

**记住核心思想：**
- **原型链继承**：子类原型 = 父类实例 → 共享引用类型 ❌
- **借用构造函数**：call 调用父类 → 无法继承原型方法 ❌
- **组合继承**：原型链 + 构造函数 → 调用两次父类 ⚠️
- **寄生组合继承**：Object.create + call → 只调用一次，最优 ✅
- **ES6 class**：语法糖，底层是寄生组合继承 ✅✅

**关键知识点回顾：**
- `prototype`：函数特有的属性，指向原型对象
- `__proto__`：实例特有的属性，指向构造函数的 prototype
- `constructor`：原型对象上的属性，指向构造函数本身
- `Object.create()`：创建对象并指定原型，不调用构造函数
- `call/apply`：改变 this 指向，实现构造函数借用
- `super`：ES6 中调用父类构造函数/方法的语法糖

**适用场景：**
- ✅ 大型项目 → 使用 ES6 class
- ✅ 需要兼容旧浏览器 → 使用寄生组合继承
- ✅ 简单对象扩展 → 使用 Object.create
- ✅ 面试考察 → 理解五种方式的原理和区别
- ❌ 不使用原型链继承（引用类型共享问题）
- ❌ 不使用借用构造函数（无法继承原型方法）

---

**掌握 JavaScript 继承的核心是理解原型链机制。五种方式从简单到复杂，从有缺陷到完善，最终演变为 ES6 class 这个优雅的语法糖。无论哪种方式，底层都是对象通过 `__proto__` 连接形成的链条。**
