# 节流 (Throttle)

## 一、节流是什么？

先理解一个生活场景：你在玩一个射击游戏，机枪的射速限制是每秒10发。即使你每秒狂按鼠标100次，子弹也只能以每秒10发的固定频率射出。

**节流就是：事件触发后，规定时间内只执行一次函数。如果在时间间隔内多次触发，只有第一次（或最后一次）生效。**

它与防抖的关键区别：
- **防抖**：频繁触发时，只执行最后一次（“最后一个人说了算”）
- **节流**：频繁触发时，按固定频率执行（“控制节奏，按点执行”）

## 二、代码逐行解析

```javascript
/**
 * 题目：手写节流函数 throttle
 * 要求：
 * 1. 在一段时间内只允许函数执行一次
 * 2. 分别用时间戳版和定时器版实现
 * 3. 返回的函数支持 cancel() 方法
 */

// ----- 版本一：时间戳版（第一次立即执行，最后一次不保证执行）-----
function throttleTimestamp(fn, delay) {
    let lastTime = 0  // 上一次执行的时间戳
    
    const exec = function (...args) {
        const now = Date.now()
        const context = this
        
        // 如果距离上次执行超过了delay，就执行
        if (now - lastTime >= delay) {
            fn.apply(context, args)
            lastTime = now  // 更新上次执行时间
        }
    }
    
    exec.cancel = () => {
        lastTime = 0  // 重置时间戳
    }
    
    return exec
}

// ----- 版本二：定时器版（第一次延迟执行，最后一次保证执行）-----
function throttleTimer(fn, delay) {
    let timer = null
    
    const exec = function (...args) {
        const context = this
        
        // 如果没有计时器，就启动一个
        if (!timer) {
            timer = setTimeout(() => {
                fn.apply(context, args)
                timer = null  // 执行完清空计时器
            }, delay)
        }
    }
    
    exec.cancel = () => {
        if (timer) {
            clearTimeout(timer)
            timer = null
        }
    }
    
    return exec
}

// ----- 版本三：完整版（支持leading和trailing配置）-----
function throttle(fn, delay, options = { leading: true, trailing: true }) {
    let timer = null      // trailing定时器
    let lastExecTime = 0  // 上次执行时间戳
    let lastArgs = null    // 最后一次调用的参数
    let lastContext = null // 最后一次调用的this
    
    const { leading, trailing } = options
    
    const exec = function (...args) {
        const now = Date.now()
        lastArgs = args
        lastContext = this
        
        // 如果是第一次执行且leading为false，初始化lastExecTime
        if (lastExecTime === 0 && !leading) {
            lastExecTime = now
        }
        
        const remaining = delay - (now - lastExecTime)
        
        if (remaining <= 0) {
            // 可以执行了
            if (timer) {
                clearTimeout(timer)
                timer = null
            }
            fn.apply(lastContext, lastArgs)
            lastExecTime = now
            lastArgs = null
            lastContext = null
        } else if (!timer && trailing) {
            // 还没到时间，但需要设置最后一次执行的定时器
            timer = setTimeout(() => {
                fn.apply(lastContext, lastArgs)
                lastExecTime = Date.now()
                timer = null
                lastArgs = null
                lastContext = null
            }, remaining)
        }
    }
    
    exec.cancel = () => {
        if (timer) {
            clearTimeout(timer)
            timer = null
        }
        lastExecTime = 0
        lastArgs = null
        lastContext = null
    }
    
    return exec
}

// ---------- 测试用例 ----------
// 时间戳版
const log1 = throttleTimestamp(() => console.log('时间戳版执行', Date.now()), 1000)
log1() // 立即执行
log1() // 1秒内被忽略
setTimeout(() => log1(), 500)  // 被忽略
setTimeout(() => log1(), 1200) // 执行（距离上次超过1秒）

// 定时器版
const log2 = throttleTimer(() => console.log('定时器版执行', Date.now()), 1000)
log2() // 1秒后执行
log2() // 被忽略（已经有计时器）
log2() // 被忽略

// 完整版测试
const log3 = throttle(() => console.log('完整版执行'), 1000, { leading: true, trailing: true })
log3() // 立即执行
log3() // 被忽略
setTimeout(() => log3(), 500)  // 被忽略
setTimeout(() => log3(), 1200) // 执行（距离上次超过1秒）
```

## 三、API详解

| API | 作用 | 在节流中的用途 |
|-----|------|----------------|
| `Date.now()` | 获取当前毫秒时间戳 | 计算时间差，判断是否可以执行 |
| `setTimeout(fn, ms)` | 延迟执行 | trailing模式：最后一次延迟执行 |
| `clearTimeout(id)` | 取消延迟执行 | 清理定时器 |
| `.apply(this, [args])` | 调用函数并指定this和参数 | 保持this指向和参数传递 |

## 四、三种实现对比

### 时间戳版执行流程
```
触发1: 立即执行✅，记录时间t1
触发2: 检查now - t1 < delay，不执行❌
触发3: 检查now - t1 < delay，不执行❌
t1+delay: 可以再次执行
特点：第一次立即执行，最后一次不保证执行
```

### 定时器版执行流程
```
触发1: 启动计时器，等待delay
触发2: 已有计时器，不执行❌
触发3: 已有计时器，不执行❌
delay后: 执行✅，清空计时器
特点：第一次延迟执行，保证最后一次会执行
```

### 完整版执行流程
```
触发1: leading=true，立即执行✅
触发2: remaining>0，判断trailing=true，设置定时器
触发3: 更新参数，准备延迟执行
delay后: 执行最后一次调用✅

特点：可配置leading和trailing，兼具两者优点
```

## 五、代码追踪示例

```javascript
// 时间戳版追踪
const throttleFn = throttleTimestamp(() => console.log('执行'), 1000);

throttleFn(); // t=0ms: now=0, lastTime=0，执行✅，lastTime=0
throttleFn(); // t=100ms: now=100, 100-0 < 1000，不执行❌
throttleFn(); // t=200ms: now=200, 200-0 < 1000，不执行❌
// t=1000ms后: 可以再次执行

// 定时器版追踪
const throttleFn2 = throttleTimer(() => console.log('执行'), 1000);

throttleFn2(); // t=0ms: timer=null，启动定时器，timer=1
throttleFn2(); // t=100ms: timer不为空，不执行❌
throttleFn2(); // t=200ms: timer不为空，不执行❌
// t=1000ms: 定时器触发，执行✅，timer=null
```

## 六、常见应用场景

```javascript
// 1. 滚动加载（无限滚动）
window.addEventListener('scroll', throttle(() => {
    if (isBottom()) {
        loadMoreData();
    }
}, 200));

// 2. 按钮点击频率限制（防止刷接口）
const sendSms = throttle(() => {
    sendVerificationCode();
}, 3000, { leading: true, trailing: false });
// 点击立即发送，3秒内再次点击无效

// 3. 窗口resize重新计算布局
window.addEventListener('resize', throttle(() => {
    recalculateLayout();
}, 100));

// 4. 游戏中的射击频率
const shoot = throttle(() => {
    fireBullet();
}, 200, { leading: true, trailing: false });
// 限制射速为每秒5发
```

## 七、完整测试代码

```javascript
// 测试时间戳版
const test1 = throttleTimestamp(() => console.log('时间戳版'), 1000);
test1(); // 立即输出
test1(); // 不输出
setTimeout(() => test1(), 500);  // 不输出
setTimeout(() => test1(), 1200); // 输出（1.2秒后）

// 测试定时器版
const test2 = throttleTimer(() => console.log('定时器版'), 1000);
test2(); // 1秒后输出
test2(); // 不输出
setTimeout(() => test2(), 500);  // 不输出（已有计时器）
setTimeout(() => test2(), 1500); // 2.5秒后输出（上一次执行完1秒后）

// 测试cancel
const test3 = throttleTimestamp(() => console.log('被取消了'), 1000);
test3();
test3.cancel(); // 重置状态
test3(); // 又会立即执行一次（因为lastTime=0）
```

## 八、闭包机制深度剖析

### 8.1 时间戳版的闭包变量

```javascript
function throttleTimestamp(fn, delay) {
    let lastTime = 0;  // ← 闭包变量，保存上次执行时间
    
    return function(...args) {
        // 可以访问和修改lastTime
    }
}

const scrollHandler = throttleTimestamp(onScroll, 200);
// 内存结构：
// scrollHandler ──闭包引用──> { lastTime: 0 }  ← 持久存在
```

### 8.2 闭包的作用

```javascript
// 场景：多个节流实例互不干扰
const scrollHandler = throttleTimestamp(() => console.log('滚动'), 200);
const resizeHandler = throttleTimestamp(() => console.log('缩放'), 200);

// 内存结构：
// scrollHandler ──闭包──> { lastTime: scrollLastTime }
// resizeHandler ──闭包──> { lastTime: resizeLastTime }
// 彼此独立，互不影响
```

## 九、this和apply详解（与防抖相同）

### 9.1 为什么要保存this？

```javascript
// 场景：在DOM元素事件中使用
const button = document.querySelector('button');
button.onclick = throttleTimestamp(function() {
    console.log(this);  // 希望指向button元素
    this.disabled = true;
}, 1000);

// 使用apply保存this：
fn.apply(context, args);  // context = button，正确指向
```

### 9.2 时间戳版 vs 定时器版的this处理

```javascript
// 时间戳版：直接使用apply
if (now - lastTime >= delay) {
    fn.apply(context, args);  // context是调用时的this
    lastTime = now;
}

// 定时器版：需要在定时器回调中使用保存的this
if (!timer) {
    timer = setTimeout(() => {
        fn.apply(context, args);  // 闭包捕获了context
        timer = null;
    }, delay);
}
```

## 十、时间戳版与定时器版对比

| 特性 | 时间戳版 | 定时器版 | 完整版 |
|------|----------|----------|--------|
| 首次执行 | 立即执行 | 延迟delay后 | 可配置 |
| 末次执行 | 不保证 | 保证执行 | 可配置 |
| 内存占用 | 低（只有时间戳） | 中（有定时器） | 高（定时器+时间戳） |
| 实现复杂度 | ⭐ | ⭐⭐ | ⭐⭐⭐⭐ |
| 适用场景 | 需要立即响应 | 需要最后一次执行 | 通用 |

## 十一、完整版leading和trailing详解

```javascript
// leading=true, trailing=true (默认)
// 第一次立即执行，停止触发后delay再执行一次
throttle(fn, 1000, { leading: true, trailing: true })
// 触发序列: 触-触-触-停
// 执行时机: 立----等1秒----执

// leading=true, trailing=false  
// 第一次立即执行，最后一次不执行
throttle(fn, 1000, { leading: true, trailing: false })
// 执行时机: 立(后续都忽略)

// leading=false, trailing=true
// 第一次不执行，停止触发后delay执行一次
throttle(fn, 1000, { leading: false, trailing: true })
// 触发序列: 触-触-触-停
// 执行时机: ----等1秒----执

// leading=false, trailing=false
// 完全不执行（没啥用）
```

## 十二、常见错误与调试

### 12.1 错误1：忘记更新时间戳

```javascript
// 错误示例
if (now - lastTime >= delay) {
    fn.apply(context, args);
    // 忘记更新 lastTime = now
}
// 结果：每次调用都会执行，节流失效

// 正确做法
if (now - lastTime >= delay) {
    fn.apply(context, args);
    lastTime = now;  // 必须更新
}
```

### 12.2 错误2：定时器版中this丢失

```javascript
// 错误示例
if (!timer) {
    timer = setTimeout(function() {
        fn.apply(this, args);  // 普通函数的this指向window
        timer = null;
    }, delay);
}

// 正确做法：保存context
const context = this;
if (!timer) {
    timer = setTimeout(() => {
        fn.apply(context, args);  // 箭头函数继承外层this
        timer = null;
    }, delay);
}
```

### 12.3 错误3：React中未清理

```javascript
// 错误示例
useEffect(() => {
    const handleScroll = throttle(() => fetchData(), 500);
    window.addEventListener('scroll', handleScroll);
    // 忘记清理：组件卸载后还在执行
}, []);

// 正确做法
useEffect(() => {
    const handleScroll = throttle(() => fetchData(), 500);
    window.addEventListener('scroll', handleScroll);
    return () => {
        window.removeEventListener('scroll', handleScroll);
        handleScroll.cancel();  // 清理定时器
    };
}, []);
```

## 十三、性能优化建议

```javascript
// 1. 合理设置delay
// 滚动事件：100-200ms（流畅体验）
// 按钮防连点：500-1000ms（防止误操作）
// 游戏射击：100-300ms（游戏手感）

// 2. 根据场景选择版本
// 滚动需要立即响应：时间戳版
// 确保最后一次一定执行：定时器版
// 复杂需求：完整版

// 3. 避免频繁创建函数
// 不好
component.addEventListener('scroll', () => {
    throttle(fn, 100)();  // 每次滚动都创建新的节流函数
});

// 好
const throttledFn = throttle(fn, 100);
component.addEventListener('scroll', throttledFn);
```

## 十四、防抖 vs 节流 对比总结

| 维度 | 防抖 (Debounce) | 节流 (Throttle) |
|------|----------------|-----------------|
| 核心思想 | 只执行最后一次 | 固定频率执行 |
| 执行时机 | 停止触发后delay毫秒 | 每delay毫秒至少一次 |
| 生活类比 | 电梯关门 | 机枪射速 |
| 适用场景 | 输入搜索、表单验证 | 滚动加载、resize、按钮限频 |
| 首次执行 | 可配置immediate | 时间戳版立即/定时器版延迟 |
| 末次执行 | 保证执行 | 可配置trailing |
| 调用频率 | 非常高频时只执行1次 | 高频时仍会执行多次 |

## 十五、总结

**记住核心思想：节流就是“控制节奏，按点执行”，适用于滚动加载、窗口resize、按钮限频等需要限制执行频率的场景。**

**关键知识点回顾：**
- 时间戳版：记录上次执行时间，时间到了就执行
- 定时器版：用计时器锁住，执行完才解锁
- leading：控制第一次是否立即执行
- trailing：控制最后一次是否延迟执行
- cancel：清理定时器，重置状态

**适用场景对比：**
- ✅ 滚动无限加载 → 节流
- ✅ 窗口resize重绘 → 节流
- ✅ 按钮防止连点 → 节流
- ✅ 搜索框实时查询 → 防抖
- ✅ 表单实时验证 → 防抖
- ❌ 需要每次都响应的 → 两者都不适合
