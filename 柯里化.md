# 柯里化

## 一、柯里化是什么？

先理解一个生活场景：你去奶茶店点单。店员不会让你一次性说完“我要一杯少冰半糖去珍珠的大杯波霸奶茶”，而是分步骤问你：
- 第一步："要什么茶？" → "波霸奶茶"
- 第二步："甜度？" → "半糖"
- 第三步："冰量？" → "少冰"
- 第四步："加料？" → "去珍珠"
- 第五步："杯型？" → "大杯"

**柯里化就是：把一个接收多个参数的函数，拆解成一系列只接收一个（或部分）参数的函数链条，参数收集够了才执行。**

### 核心疑问解答

**Q1：为什么 `curriedAdd(1,2,3)` 可以直接调用？**

因为柯里化函数内部做了判断：只要收集的参数个数 ≥ 原函数需要的参数个数，就立即执行原函数。一次性传3个参数，3 ≥ 3，所以直接执行。

**Q2：为什么 `curriedAdd(1)(2)(3)` 可以连续括号调用？**

因为 `curriedAdd(1)` 参数不够（1 < 3），返回的是一个**新函数**。只要是函数，后面就可以跟 `()` 调用。第二次调用 `(2)` 还是参数不够，再返回新函数。第三次调用 `(3)` 参数够了，执行原函数返回结果（数字6）。

**Q3：`newArgs` 从哪里来？为什么不需要 `let/const` 声明？**

`newArgs` 是函数的**形参（parameter）**。形参是函数定义时自带的“变量槽位”，JavaScript 引擎在调用函数时自动创建并赋值，不需要手动声明。就像 `function add(a,b)` 中的 `a` 和 `b`，你从来不需要写 `let a`。

**Q4：`fn.length` 是什么？数组才有 length，函数怎么也有？**

JavaScript 中**函数也是对象**，对象可以有属性。`length` 是函数对象的**内置属性**，代表函数的**形参个数**。这是语言设计的一部分，每个函数都自带这个属性。

📚 **MDN 官方文档：**
- Function.length：https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Function/length
- Rest parameters：https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Functions/rest_parameters
- Function.apply：https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Function/apply
- 闭包：https://developer.mozilla.org/en-US/docs/Web/JavaScript/Closures

---

## 二、代码逐行解析

```javascript
/**
 * 题目：实现函数柯里化 curry
 * 要求：
 * 1. 将接受多个参数的函数转换为一系列接受单个（或部分）参数的函数
 * 2. 当收集的参数数量达到原函数参数数量时自动执行
 * 3. 支持一次传入多个参数
 */

function curry(fn) {
    // 获取原函数期望的参数个数（形参数量）
    // 例：function add(a,b,c){} → fn.length = 3
    const arity = fn.length
    
    // 创建闭包函数，用于收集参数
    // ...args 是剩余参数语法，将传入的所有参数收集为数组
    function curried(...args) {
        // args.length 是【已经收集到的实参个数】
        // arity 是原函数【期望的形参个数】
        if (args.length >= arity) {
            // 参数够了！执行原函数，并传递所有收集的参数
            // apply 的作用：保持 this 指向，并把数组 args 展开传给 fn
            return fn.apply(this, args)
        } else {
            // 参数不够，返回一个新函数继续收集参数
            // 这个新函数的 ...newArgs 就是【下一次调用时传入的参数】
            return function (...newArgs) {
                // args.concat(newArgs) 合并已收集的和新传入的
                // 递归调用 curried，继续判断参数是否足够
                return curried.apply(this, args.concat(newArgs))
            }
        }
    }
    
    return curried
}
```

---

## 三、彻底理解：形参 vs 实参 vs `fn.length`

### 3.1 形参和实参的区别

```javascript
// 函数定义时的 a, b, c 是【形参】（Parameter）
// 形参不需要 let/const 声明，函数定义时自动创建
function add(a, b, c) {
    return a + b + c
}

// 函数调用时的 1, 2, 3 是【实参】（Argument）
add(1, 2, 3)  // 1,2,3 是实参
```

| 概念 | 英文 | 含义 | 例子 |
|------|------|------|------|
| 形参 | Parameter | 定义函数时写的参数名 | `function add(a,b,c)` 中的 a,b,c |
| 实参 | Argument | 调用函数时传入的实际值 | `add(1,2,3)` 中的 1,2,3 |
| `fn.length` | Function.length | **形参**的个数 | `add.length === 3` |
| `args.length` | Arguments.length | **实参**的个数 | 调用时传入几个就是几 |

### 3.2 为什么函数有 `length` 属性？

**JavaScript 中函数也是对象**，对象可以有属性。`length` 是函数对象的内置属性。

```javascript
// 证明函数是对象
function myFunc(a, b) {
    return a + b
}

// 函数可以有属性（就像普通对象一样）
myFunc.author = '张三'
myFunc.version = '1.0'

console.log(myFunc.length)   // 2（内置属性，形参个数）
console.log(myFunc.name)     // 'myFunc'（内置属性，函数名）
console.log(myFunc.author)   // '张三'（自定义属性）
```

### 3.3 `fn.length` 的特殊情况

```javascript
// 1. 有默认参数：只计算默认参数之前的形参
function greet(name, greeting = 'Hello') {}
greet.length  // 1

// 2. 有剩余参数：剩余参数不计入
function sum(a, b, ...rest) {}
sum.length  // 2

// 3. 零参数函数
function sayHi() {}
sayHi.length  // 0
```

---

## 四、彻底理解：`newArgs` 从哪来？为什么不需要声明？

### 4.1 形参不需要声明——它是函数自带的“变量”

```javascript
// 你熟悉的写法
function add(a, b) {  // a 和 b 是形参，从来没写过 let a = ...
    return a + b      // 直接用，不报错
}

// 对比：普通变量必须声明
function test() {
    // x = 10      ❌ 报错（未声明的变量，严格模式下）
    let x = 10     ✅ 必须先声明
    return x
}
```

**为什么形参不需要声明？** 因为形参是函数的“签名”部分，JavaScript 引擎在解析函数时自动创建这些变量。

### 4.2 函数的形参就是函数的“局部变量”

```javascript
// 你写的代码
function curried(...args) {
    // ...args 就是形参
}

// 引擎内部的理解（伪代码）
function curried() {
    // 引擎自动创建 args 变量，并赋值
    // let args = Array.from(arguments)
    // 然后你可以在函数体内使用 args
}

// 调用时
curried(1, 2, 3)
// 引擎执行：args = [1, 2, 3]
```

### 4.3 完整参数来源追踪

```javascript
function curry(fn) {
    const arity = fn.length
    
    function curried(...args) {  // args 来自【第一次】调用时传入的值
        if (args.length >= arity) {
            return fn.apply(this, args)
        } else {
            // 返回一个新函数，这个函数的形参叫 newArgs
            return function (...newArgs) {  // newArgs 是【下一次】调用时传入的值
                return curried.apply(this, args.concat(newArgs))
            }
        }
    }
    
    return curried
}

const curriedAdd = curry((a,b,c) => a+b+c)

// 调用链追踪
curriedAdd(1)(2)(3)

// 第1次调用：curriedAdd(1)
// └─ curried 函数执行，args = [1]（1是实参，赋值给形参args）
//    └─ 参数不够，返回新函数：(newArgs) => curried(...)

// 第2次调用：上一步返回的函数(2)
// └─ 形参 newArgs 被赋值为 [2]（2是实参，赋值给形参newArgs）
//    └─ curried.apply(this, [1].concat([2])) → curried(1,2)

// 第3次调用：上一步返回的函数(3)
// └─ 形参 newArgs 被赋值为 [3]
//    └─ curried(1,2,3) → 参数够，执行原函数 → 6
```

---

## 五、测试用例

```javascript
// 测试1：基本功能
function add(a, b, c) {
    return a + b + c
}

const curriedAdd = curry(add)
console.log(curriedAdd(1)(2)(3))        // 6
console.log(curriedAdd(1, 2)(3))        // 6
console.log(curriedAdd(1)(2, 3))        // 6
console.log(curriedAdd(1, 2, 3))        // 6

// 测试2：字符串拼接
function greet(greeting, name, punctuation) {
    return `${greeting}, ${name}${punctuation}`
}

const curriedGreet = curry(greet)
console.log(curriedGreet('Hello')('World')('!'))        // Hello, World!
console.log(curriedGreet('Hi', 'John')('.'))            // Hi, John.
console.log(curriedGreet('Hey')('Alice', '?'))          // Hey, Alice?

// 测试3：乘法计算器
function multiply(x, y, z) {
    return x * y * z
}

const curriedMultiply = curry(multiply)
const double = curriedMultiply(2)      // 固定第一个参数为2
const triple = curriedMultiply(3)      // 固定第一个参数为3

console.log(double(5)(10))              // 2*5*10 = 100
console.log(triple(4)(5))              // 3*4*5 = 60
console.log(curriedMultiply(2, 3, 4))  // 24

// 测试4：折扣计算（实际业务场景）
function calculatePrice(price, discount, tax) {
    const afterDiscount = price * (1 - discount)
    const finalPrice = afterDiscount * (1 + tax)
    return Math.round(finalPrice * 100) / 100
}

const curriedPrice = curry(calculatePrice)
const withDiscount10 = curriedPrice(100)(0.1)  // 固定价格100，折扣10%
console.log(withDiscount10(0.05))               // 税率5% → 94.5
console.log(withDiscount10(0.08))               // 税率8% → 93.6

// 测试5：验证中间返回值类型
const step1 = curriedAdd(1)
console.log(typeof step1)  // 'function' ← 参数不够，返回函数

const step2 = step1(2)
console.log(typeof step2)  // 'function' ← 还是不够

const step3 = step2(3)
console.log(typeof step3)  // 'number' ← 参数够了，返回数字6
```

---

## 六、执行流程图解

### 场景1：`curriedAdd(1)(2)(3)` 分三次调用

```
调用1: curriedAdd(1)
┌─────────────────────────────────────────┐
│ curried([1]) 执行                        │
│ args = [1], args.length = 1              │
│ 1 >= 3? false                            │
│ 返回: function(newArgs) {                │
│         curried([1].concat(newArgs))     │
│       }                                  │
└─────────────────────────────────────────┘
                    ↓
调用2: 返回的函数(2)
┌─────────────────────────────────────────┐
│ newArgs = [2]                            │
│ 合并: [1].concat([2]) = [1,2]            │
│ 递归调用 curried([1,2])                  │
│ args = [1,2], args.length = 2            │
│ 2 >= 3? false                            │
│ 返回: function(newArgs) {                │
│         curried([1,2].concat(newArgs))   │
│       }                                  │
└─────────────────────────────────────────┘
                    ↓
调用3: 返回的函数(3)
┌─────────────────────────────────────────┐
│ newArgs = [3]                            │
│ 合并: [1,2].concat([3]) = [1,2,3]        │
│ 递归调用 curried([1,2,3])                │
│ args = [1,2,3], args.length = 3          │
│ 3 >= 3? true                             │
│ 执行: fn.apply(this, [1,2,3])            │
│ 返回: 6                                  │
└─────────────────────────────────────────┘
```

### 场景2：`curriedAdd(1,2)(3)` 两次调用

```
调用1: curriedAdd(1,2)
┌─────────────────────────────────────────┐
│ curried([1,2]) 执行                      │
│ args = [1,2], args.length = 2            │
│ 2 >= 3? false                            │
│ 返回: function(newArgs) {                │
│         curried([1,2].concat(newArgs))   │
│       }                                  │
└─────────────────────────────────────────┘
                    ↓
调用2: 返回的函数(3)
┌─────────────────────────────────────────┐
│ newArgs = [3]                            │
│ 合并: [1,2].concat([3]) = [1,2,3]        │
│ curried([1,2,3]) 执行                    │
│ 3 >= 3? true → 执行 fn → 返回 6          │
└─────────────────────────────────────────┘
```

### 场景3：`curriedAdd(1,2,3)` 一次调用

```
调用: curriedAdd(1,2,3)
┌─────────────────────────────────────────┐
│ curried([1,2,3]) 执行                    │
│ args = [1,2,3], args.length = 3          │
│ 3 >= 3? true                             │
│ 直接执行 fn.apply(this, [1,2,3]) → 6     │
│ 不返回函数，直接返回结果                  │
└─────────────────────────────────────────┘
```

---

## 七、API详解

| API | 作用 | 说明 |
|-----|------|------|
| `fn.length` | 获取函数的形参个数 | `function add(a,b,c){}` → `add.length = 3` |
| `...args` | 剩余参数 | 收集所有传入参数为数组 |
| `args.length` | 判断已收集的参数数量 | 决定是执行还是继续收集 |
| `fn.apply(this, args)` | 调用原函数 | 保持this指向和参数传递 |
| `args.concat(newArgs)` | 合并参数数组 | 递归收集新参数 |

---

## 八、完整代码追踪（逐步调试）

```javascript
function curry(fn) {
    const arity = fn.length
    console.log(`原函数需要${arity}个参数`)
    
    function curried(...args) {
        console.log(`当前收集参数: [${args}]，共${args.length}个`)
        
        if (args.length >= arity) {
            console.log(`参数足够！执行原函数`)
            return fn.apply(this, args)
        } else {
            console.log(`参数不够，返回新函数继续收集`)
            return function (...newArgs) {
                console.log(`新收到参数: [${newArgs}]`)
                const merged = args.concat(newArgs)
                console.log(`合并后参数: [${merged}]`)
                return curried.apply(this, merged)
            }
        }
    }
    
    return curried
}

const add = (a, b, c) => a + b + c
const curriedAdd = curry(add)

console.log('--- 逐步调用 ---')
curriedAdd(1)(2)(3)

// 输出：
// 原函数需要3个参数
// --- 逐步调用 ---
// 当前收集参数: [1]，共1个
// 参数不够，返回新函数继续收集
// 新收到参数: [2]
// 合并后参数: [1,2]
// 当前收集参数: [1,2]，共2个
// 参数不够，返回新函数继续收集
// 新收到参数: [3]
// 合并后参数: [1,2,3]
// 当前收集参数: [1,2,3]，共3个
// 参数足够！执行原函数
// 6
```

---

## 九、常见应用场景

### 1. 参数复用（固定参数）
```javascript
const curriedMultiply = curry((x, y) => x * y)
const double = curriedMultiply(2)  // 固定第一个参数为2
const triple = curriedMultiply(3)  // 固定第一个参数为3

console.log(double(5))   // 10
console.log(triple(5))   // 15

// 实际场景：创建不同折扣的优惠计算器
const curriedDiscount = curry((rate, price) => price * (1 - rate))
const tenPercentOff = curriedDiscount(0.1)   // 9折
const twentyPercentOff = curriedDiscount(0.2) // 8折

console.log(tenPercentOff(100))    // 90
console.log(twentyPercentOff(100)) // 80
```

### 2. 延迟执行
```javascript
const curriedLog = curry((level, timestamp, message) => {
    console.log(`[${level}] ${timestamp} - ${message}`)
})

const getCurrentTime = () => new Date().toLocaleTimeString()
const infoLog = curriedLog('INFO')(getCurrentTime())
const errorLog = curriedLog('ERROR')(getCurrentTime())

infoLog('应用启动')    // [INFO] 14:30:25 - 应用启动
errorLog('连接失败')   // [ERROR] 14:30:26 - 连接失败
```

### 3. 函数组合（配合map、filter）
```javascript
const curriedMap = curry((fn, arr) => arr.map(fn))
const curriedFilter = curry((fn, arr) => arr.filter(fn))

const numbers = [1, 2, 3, 4, 5, 6]

const evenNumbers = curriedFilter(n => n % 2 === 0)
const doubleNumbers = curriedMap(n => n * 2)

console.log(doubleNumbers(evenNumbers(numbers)))  // [4, 8, 12]
```

### 4. 部分应用（Partial Application）
```javascript
const curriedRequest = curry((method, url, data) => {
    console.log(`发送${method}请求到${url}`, data)
    return { method, url, data }
})

const getRequest = curriedRequest('GET')
const postRequest = curriedRequest('POST')
const getUserApi = getRequest('/api/user')

getUserApi({ id: 1 })  // GET /api/user {id:1}
```

---

## 十、闭包机制深度剖析

### 10.1 什么是柯里化的闭包？（内存视角）

```javascript
function curry(fn) {
    const arity = fn.length  // ← 闭包变量1
    
    function curried(...args) {  // ← 闭包变量2（每次调用创建新的args）
        if (args.length >= arity) {
            return fn.apply(this, args)
        } else {
            return function (...newArgs) {  // ← 又创建新闭包
                return curried.apply(this, args.concat(newArgs))
            }
        }
    }
    return curried
}

const curriedAdd = curry((a,b,c) => a+b+c)  // 内存：curriedAdd ──闭包──> { arity:3 }
const addWith1 = curriedAdd(1)  // 内存：addWith1 ──闭包──> { args:[1], 指向curried }
```

### 10.2 多层闭包嵌套的调用栈

```javascript
curriedAdd(1)(2)(3)

// 栈帧1: curriedAdd(1) → 创建闭包1，args=[1] → 返回函数A
// 栈帧2: 函数A(2) → 创建闭包2，合并args=[1,2] → 返回函数B
// 栈帧3: 函数B(3) → 创建闭包3，合并args=[1,2,3] → 执行fn → 返回6
```

---

## 十一、进阶版本：支持占位符

```javascript
const _ = Symbol('placeholder')

function curryWithPlaceholder(fn) {
    const arity = fn.length
    
    function curried(...args) {
        const argsWithoutPlaceholder = args.filter(arg => arg !== _)
        
        if (argsWithoutPlaceholder.length >= arity && !args.includes(_)) {
            return fn.apply(this, argsWithoutPlaceholder)
        }
        
        return function (...newArgs) {
            let finalArgs = []
            let newIdx = 0
            
            for (let arg of args) {
                if (arg === _ && newIdx < newArgs.length) {
                    finalArgs.push(newArgs[newIdx++])
                } else {
                    finalArgs.push(arg)
                }
            }
            
            while (newIdx < newArgs.length) {
                finalArgs.push(newArgs[newIdx++])
            }
            
            return curried(...finalArgs)
        }
    }
    
    return curried
}

const add3 = (a, b, c) => a + b + c
const curriedAdd3 = curryWithPlaceholder(add3)

console.log(curriedAdd3(_, 2, 3)(1))      // 6
console.log(curriedAdd3(1, _, 3)(2))      // 6
console.log(curriedAdd3(1, _, _)(2, 3))   // 6
```

---

## 十二、常见错误与调试

### 12.1 this指向丢失

```javascript
const obj = {
    value: 10,
    add: function(a, b) {
        return this.value + a + b
    }
}

const curriedAdd = curry(obj.add)
console.log(curriedAdd(1)(2))  // NaN（this指向window）

// 解决方案：bind绑定this
const boundCurriedAdd = curry(obj.add.bind(obj))
console.log(boundCurriedAdd(1)(2))  // 13
```

### 12.2 忘记处理零参数函数

```javascript
function sayHello() { return 'Hello' }

// 改进版：零参数直接执行
function curry(fn) {
    const arity = fn.length === 0 ? 0 : fn.length
    
    function curried(...args) {
        if (arity === 0) {
            return fn.apply(this, args)
        }
        if (args.length >= arity) {
            return fn.apply(this, args)
        } else {
            return function (...newArgs) {
                return curried.apply(this, args.concat(newArgs))
            }
        }
    }
    return curried
}
```

### 12.3 参数顺序混乱导致难以复用

```javascript
// 不好的设计：可配置参数放在前面
const bad = curry((a, b, op) => op === 'add' ? a + b : a * b)

// 好的设计：可配置参数放在后面
const good = curry((op, a, b) => op === 'add' ? a + b : a * b)
const add = good('add')
const multiply = good('multiply')
```

---

## 十三、性能优化建议

```javascript
// 1. 避免过深的柯里化链
const deepCurry = curry((a,b,c,d,e,f) => a+b+c+d+e+f)
deepCurry(1,2,3,4,5,6)  // ✅ 一次性传参，只创建1个中间判断
// deepCurry(1)(2)(3)(4)(5)(6)  // ❌ 创建5个中间函数

// 2. 使用缓存避免重复创建函数
const curriedMultiply = curry((x, y) => x * y)
const doubleCache = new Map()

function getDouble(num) {
    if (!doubleCache.has(num)) {
        doubleCache.set(num, curriedMultiply(num))
    }
    return doubleCache.get(num)
}

// 3. 提前计算arity
function curry(fn) {
    const arity = fn.length  // ← 只在初始化时计算一次
    // 不要在curried内部重复访问fn.length
}
```

---

## 十四、与防抖的联系（对比学习）

| 特性 | 防抖 | 柯里化 |
|------|------|--------|
| 核心思想 | 延迟执行，重置计时 | 逐步收集参数，够数执行 |
| 闭包作用 | 保存timer状态 | 保存已收集的参数数组 |
| 返回内容 | 统一的防抖函数 | 可能是新函数或执行结果 |
| 递归调用 | 无递归 | 参数不够时递归返回新函数 |
| 常见应用 | 输入框、滚动事件 | 参数复用、函数组合 |

---

## 十五、总结

**记住核心思想：柯里化就是“分批买票，买够了上车”，把一次说完的话分多次说，说够了再执行。**

### 常见疑惑快速问答

| 疑问 | 答案 |
|----------|------|
| 为什么 `curriedAdd(1,2,3)` 可以直接调用？ | 参数一次性给够（3≥3），直接执行原函数 |
| 为什么 `(3)` 能跟在后面调用？ | `curriedAdd(1)` 返回的是函数，函数就能被调用 |
| `newArgs` 从哪里来？ | 是返回的那个函数的**形参**，调用时由实参赋值 |
| 为什么 `newArgs` 不需要声明？ | 形参是函数自带的，不需要 `let/const` |
| `fn.length` 是什么？ | 函数定义时的**形参个数**，函数对象的内置属性 |
| 函数为什么有 `length`？ | JavaScript 中函数也是对象，`length` 是其内置属性 |

### 关键知识点回顾

- `fn.length`：获取原函数需要的参数个数
- 闭包链：每层柯里化都保存已收集的参数
- 递归思想：参数不够就递归返回新函数
- `args.concat(newArgs)`：合并新旧参数
- `apply(this, args)`：保持this指向和参数传递

### 适用场景

- ✅ 参数复用（创建特定功能的函数）
- ✅ 延迟执行（配置和调用分离）
- ✅ 函数组合（数据管道处理）
- ✅ 配置项很多且可分步传入的场景
- ❌ 需要立即执行所有参数的场景
- ❌ 参数数量动态变化的场景

### 测试用例汇总

```javascript
// 基础测试
const add = (a,b,c) => a+b+c
const curriedAdd = curry(add)
console.log(curriedAdd(1)(2)(3))        // 6
console.log(curriedAdd(1,2)(3))         // 6
console.log(curriedAdd(1)(2,3))         // 6
console.log(curriedAdd(1,2,3))          // 6

// 部分应用测试
const multiply = (a,b) => a * b
const double = curry(multiply)(2)
console.log(double(5))                  // 10

// 复杂场景测试
const obj = { prefix: 'Value: ' }
function format(prefix, num) {
    return this.prefix + num
}
const curriedFormat = curry(format.bind(obj))
const withPrefix = curriedFormat(obj.prefix)
console.log(withPrefix(100))            // Value: 100
```
