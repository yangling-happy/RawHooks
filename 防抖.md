# 防抖

## 一、防抖是什么？

先理解一个生活场景：你在电梯里，有人要进来就按开门键。每次有人按开门键，电梯就重新计时10秒关门。如果一直有人进来，电梯就一直不关门。

**防抖就是：事件触发后，等待一段时间执行，如果等待期间又触发了，就重新计时。**

## 二、代码逐行解析

### 1. 函数签名
```javascript
function debounce(fn, delay, immediate = false)
```
- `fn`：你要执行的函数（比如发送请求、打印日志）
- `delay`：等待时间（毫秒）
- `immediate`：是否立即执行（默认false）

### 2. 闭包变量
```javascript
let timer = null;  // 保存计时器ID，闭包特性让它在多次调用间保持
```

### 3. 返回的执行函数
```javascript
const exec = function (...args) {
    const context = this;  // 保存调用时的this指向
    
    if (timer) clearTimeout(timer);  // 清除上一次的计时器
    
    if (immediate && !timer) {  // 立即执行模式且没有计时器时才执行
        fn.apply(context, args);
    }
    
    timer = setTimeout(() => {
        if (!immediate) {  // 非立即执行模式才执行
            fn.apply(context, args);
        }
        timer = null;  // 执行完重置计时器
    }, delay);
}
```

### 4. cancel方法
```javascript
exec.cancel = () => {
    clearTimeout(timer);
}
```

## 三、API详解

| API | 作用 | 常见用途 |
|-----|------|----------|
| `setTimeout(fn, ms)` | 延迟ms毫秒执行fn | 延迟请求、动画延迟 |
| `clearTimeout(id)` | 取消延迟执行 | 重置计时器 |
| `.apply(this, [args])` | 调用函数并指定this和参数 | 保持this指向 |
| `...args` | 剩余参数语法 | 收集所有参数 |

## 四、两种模式执行流程图解

### 普通模式（immediate=false）
```
触发1: 等待...等待...执行✅
触发2: 重置计时器，重新等待...执行✅
```

### 立即模式（immediate=true）
```
触发1: 立即执行✅，然后开始等待
触发2: 取消等待，重新等（不执行）
等待结束：什么都不做（已在触发时执行）
```

## 五、代码追踪示例

```javascript
// 创建防抖函数
const log = debounce(() => console.log('执行'), 500);

log(); // 第0ms: 开始计时（取消之前的空计时器）
log(); // 第100ms: 取消300ms计时器，重新500ms计时
log(); // 第200ms: 取消400ms计时器，重新500ms计时
// 第500ms: 执行✅
```

## 六、常见应用场景

```javascript
// 搜索框输入防抖
searchInput.addEventListener('input', debounce(() => {
    fetchSearchResults(searchInput.value);
}, 300));

// 窗口resize防抖
window.addEventListener('resize', debounce(() => {
    recalculateLayout();
}, 200));

// 立即执行的引用场景（如提交按钮）
const submit = debounce(() => sendData(formData), 1000, true);
// 第一次点击立即发送，后续快速点击无效
```

## 七、完整测试代码

```javascript
// 测试普通模式
const test1 = debounce(() => console.log('1:普通模式执行'), 1000);
test1(); // 1秒后执行
setTimeout(() => test1(), 500); // 重新计时，1.5秒后执行

// 测试立即模式
const test2 = debounce(() => console.log('2:立即模式执行'), 1000, true);
test2(); // 立即执行
setTimeout(() => test2(), 500); // 重置计时器但不执行
// 观察：不会有第二次输出

// 测试cancel
const test3 = debounce(() => console.log('3:被取消了'), 1000);
test3();
test3.cancel(); // 取消执行，不会输出
```

## 八、闭包机制深度剖析

### 8.1 什么是闭包？（内存视角）

```javascript
function debounce(fn, delay, immediate) {
    let timer = null;  // ← 这个变量不会随函数执行完就销毁
    
    return function(...args) {  // ← 返回的函数"记住"了timer
        // 可以访问timer
    }
}

const log = debounce(() => console.log('hi'), 500);
// 此时内存中：
// log函数 ──闭包引用──> { timer: null }  ← 这个对象不会被垃圾回收
```

### 8.2 多个实例互不干扰

```javascript
const log1 = debounce(() => console.log('1'), 500);
const log2 = debounce(() => console.log('2'), 500);

// 内存结构：
// log1 ──闭包──> { timer: null }  ← 独立的timer
// log2 ──闭包──> { timer: null }  ← 独立的timer
// 两者互不影响
```

## 九、this和apply详解

### 9.1 为什么要保存context？

```javascript
// 场景：在对象方法中使用防抖
const obj = {
    name: 'MyObj',
    logName: debounce(function() {
        console.log(this.name);  // 希望输出 'MyObj'
    }, 500)
};

obj.logName(); 
// 如果不保存context，this会指向window，输出undefined
// 保存后：context = this = obj，正确输出'MyObj'
```

### 9.2 apply的作用

```javascript
// apply的三个作用：
// 1. 指定this指向
// 2. 传递参数数组
// 3. 立即调用函数

fn.apply(context, args);
// context: 让fn内部的this指向它
// args: 把参数数组展开传递给fn

// 等价于：
fn.call(context, ...args);
```

## 十、箭头函数与普通函数的区别

### 10.1 箭头函数的this陷阱

```javascript
// 普通函数：this动态绑定
const obj = {
    name: 'obj',
    method: debounce(function() {
        console.log(this.name);  // this指向obj
    }, 500)
};

// 箭头函数：this静态绑定（指向定义时的外层this）
const obj2 = {
    name: 'obj2',
    method: debounce(() => {
        console.log(this.name);  // this指向window（严格模式下undefined）
    }, 500)
};
```

### 10.2 什么时候用箭头函数？

```javascript
// 适合箭头函数的场景：不需要this
debounce(() => {
    console.log('简单逻辑，不需要this');
    fetchData();  // 调用其他函数
}, 500);

// 适合普通函数的场景：需要动态this
debounce(function() {
    this.innerHTML = '已点击';  // 需要指向DOM元素
    this.disabled = true;
}, 500);
```

## 十一、timer的生命周期详解

### 11.1 timer的三种状态

```javascript
// 状态1: null（未开始计时）
let timer = null;

// 状态2: 数字ID（计时中）
timer = setTimeout(() => {}, 500);  // timer = 123

// 状态3: 数字ID（已执行，待重置）
// setTimeout回调执行后将timer设为null
```

### 11.2 为什么要重置timer为null？

```javascript
// 关键代码：
timer = setTimeout(() => {
    if (!immediate) {
        fn.apply(context, args);
    }
    timer = null;  // ← 为什么需要这行？
}, delay);

// 原因：让immediate模式能再次立即执行
// 如果不重置timer = null：
// 第一次执行后timer还是数字ID
// 第二次触发时，immediate && !timer 为 false
// 将无法再次立即执行！
```

## 十二、参数传递链路追踪

### 12.1 ...args的完整传递路径

```javascript
const log = debounce(print, 500);
log('hello', 'world');

// 传递链路：
// 1. 调用log('hello', 'world')
//    └─> ...args = ['hello', 'world']
//
// 2. exec函数内部：
//    └─> fn.apply(context, args)
//        └─> args = ['hello', 'world']
//
// 3. print函数接收到：
//    └─> print('hello', 'world')
```

### 12.2 完整示例

```javascript
function print(a, b, c) {
    console.log(a, b, c);  // 输出: hello world !
}

const log = debounce(print, 500);
log('hello', 'world', '!');  // 三个参数全部传递成功
```

## 十三、完整执行时间线（毫秒级追踪）

```javascript
// 普通模式完整追踪
const log = debounce(() => console.log('执行'), 500);

// t=0ms: log()第一次调用
// ├─ timer = null
// ├─ clearTimeout(null) 无效果
// ├─ immediate && !timer: false（跳过）
// └─ 启动setTimeout，timer = 1

// t=100ms: log()第二次调用
// ├─ clearTimeout(1) 取消第一次定时器
// ├─ 启动新的setTimeout，timer = 2
// └─ 旧的定时器已取消，不会执行

// t=600ms: 第二次定时器触发
// ├─ 执行fn
// └─ timer = null

// 结果：500ms延迟后输出一次
```

## 十四、常见错误与调试

### 14.1 错误1：忘记调用cancel

```javascript
// 错误示例
const log = debounce(() => fetchData(), 500);
log();  // 启动计时器
// 组件卸载时没调用cancel，可能报错

// 正确做法（React示例）
useEffect(() => {
    const debounced = debounce(fetchData, 500);
    debounced();
    return () => debounced.cancel();  // 清理
}, []);
```

### 14.2 错误2：this指向丢失

```javascript
// 错误示例
const obj = {
    name: 'obj',
    log: debounce(function() {
        console.log(this.name);  // 输出undefined！
    }, 500)
};

const fn = obj.log;  // 取出函数
fn();  // this丢失，指向window

// 解决方案
const obj = {
    name: 'obj',
    log: debounce(function() {
        console.log(this.name);
    }, 500).bind(obj)  // 绑定this
};
```

## 十五、性能优化建议

```javascript
// 1. 合理设置delay
// 输入框：300ms（够用）
// 滚动事件：100ms（更快响应）
// 窗口resize：200ms（平衡性能）

// 2. 避免在闭包中存储大对象
// 不好
let largeData = [];
debounce(() => {
    largeData.push(bigObject);  // 闭包一直持有大对象
}, 500);

// 好
debounce(() => {
    let largeData = [];  // 用完即销毁
    largeData.push(bigObject);
}, 500);

// 3. immediate模式适合提交按钮
const submit = debounce(async () => {
    await api.post();  // 避免重复提交
}, 1000, true);
```

## 十六、总结

**记住核心思想：防抖就是“最后一个人说了算”，适用于输入搜索、窗口resize等高频操作场景。**

**关键知识点回顾：**
- 闭包：让timer在多次调用间保持状态
- apply：保持this指向和传递参数
- 箭头函数：适合简单逻辑，注意this陷阱
- timer重置：immediate模式依赖timer是否为null
- 参数传递：...args完整保留所有参数

**适用场景：**
- ✅ 搜索框实时查询
- ✅ 窗口resize重绘
- ✅ 按钮防止重复提交
- ✅ 滚动加载更多
- ❌ 需要实时反馈的场景（用节流）
- ❌ 每次触发都必须执行的场景（不用防抖）
